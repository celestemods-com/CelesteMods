import { celestemodsApi } from "../celestemodsApi";


/*const modsApi = celestemodsApi.injectEndpoints({
    baseQuery: ,
    endpoints: (build) => ({
        getMods: build.query({
            query: () =>
        })
    }),
})*/