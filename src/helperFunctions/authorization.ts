import express, { Request } from "express";
import { prisma } from "../prismaClient";
import { isErrorWithMessage } from "../errorHandling";
import { getCurrentTime } from "../helperFunctions/utils";





const generateSession = async function (res: Request) {

}





const revokeSession = async function (res: Request) {
    
}