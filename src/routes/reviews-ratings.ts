import express from "express";
const reviewsRouter = express.Router();
const mapReviewsRouter = express.Router();
const ratingsRouter = express.Router();


reviewsRouter.route("")

export {reviewsRouter, ratingsRouter};