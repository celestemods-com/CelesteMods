import { Global, createStyles } from "@mantine/core";


export function globalStyles() {
    return (
        <Global
            styles={(theme) => ({
                //global styles go here
            })}
        />
    );
}




export const useStyles = createStyles((_theme, _params, _getRef) => ({

}))